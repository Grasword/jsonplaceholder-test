name: Test

on:
  push:
    paths-ignore:
      - 'docs/**'
      - '*.md'

jobs:
  test:
    name: test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm ci
      - name: Test
        run: npm run test
